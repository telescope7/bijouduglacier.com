[Unit]
Description=Daily rebuild of the Bijou du Glacier traffic report

[Timer]
OnCalendar=daily
# Spread the run rather than hammering exactly at midnight, and catch up after
# a reboot rather than silently skipping a day.
RandomizedDelaySec=30m
Persistent=true

[Install]
WantedBy=timers.target
